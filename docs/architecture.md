# Architecture — team-tasks

```
Browser ──▶ Next.js (Front + API Routes / Vercel) ──▶ Supabase (Postgres + Auth)
                          │                                    ▲
                          └────▶ Google OAuth ─────────────────┘
```
